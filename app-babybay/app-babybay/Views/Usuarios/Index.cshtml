@model IEnumerable<app_babybay.Models.ViewBag>

@{
	ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
	<a asp-action="Create">Create New</a>
</p>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Usuario)
			</th>

			<th>
				@Html.DisplayNameFor(model => model.Usuario.Cpf)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Usuario.Telefone)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Usuario.Rua)
			</th>

			<th>
				@Html.DisplayNameFor(model => model.Usuario.Cidade)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Usuario.Email)
			</th>

			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.Usuario.Nome)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Usuario.Cpf)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Usuario.Telefone)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Usuario.Rua)
				</td>

				<td>
					@Html.DisplayFor(modelItem => item.Usuario.Cidade)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Usuario.Email)
				</td>

				<td style="display:flex;flex-direction:row">
					<a asp-action="Edit" class="btn btn-primary" asp-route-id="@item.Usuario.Id">Editar</a> |
					<a asp-action="Details" class="btn btn-primary" asp-route-id="@item.Usuario.Id">Detalhes</a> |
					<a asp-action="Delete" class="btn btn-primary" asp-route-id="@item.Usuario.Id">Deletar</a> |
					<a asp-action="Relatorio" class="btn btn-primary" asp-route-id="@item.Usuario.Id">Relatório</a>
				</td>
			</tr>
		}
	</tbody>
</table>
